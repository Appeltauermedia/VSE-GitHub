// VSE greenscreenShadowProg shader module. Classic script loaded before the main app script.
const VSE_GREENSCREEN_SHADOW_VERTEX_SHADER = "\nattribute vec2 aPos;\nvoid main(){gl_Position=vec4(aPos,0.0,1.0);}\n";
const VSE_GREENSCREEN_SHADOW_FRAGMENT_SHADER = "precision highp float;\nuniform vec2 uPixelRes;\nuniform vec2 uCssRes;\nuniform vec2 uOriginCss;\nuniform vec2 uSizeCss;\nuniform float uRot;\nuniform float uSoftness;\nuniform float uOpacity;\nvoid main(){\n  vec2 fragCss=vec2(gl_FragCoord.x/max(uPixelRes.x,1.0)*uCssRes.x,(1.0-gl_FragCoord.y/max(uPixelRes.y,1.0))*uCssRes.y);\n  vec2 p=fragCss-uOriginCss;\n  float ca=cos(-uRot), sa=sin(-uRot);\n  vec2 q=vec2(p.x*ca-p.y*sa,p.x*sa+p.y*ca);\n  vec2 h=max(uSizeCss*0.5,vec2(1.0));\n  float d=length(q/h);\n  float core=1.0-smoothstep(0.72,1.0,d);\n  float halo=1.0-smoothstep(1.0,1.0+uSoftness*1.45,d);\n  float a=max(core*0.78,halo*0.42)*uOpacity;\n  if(a<0.002){discard;}\n  gl_FragColor=vec4(vec3(0.0),a);\n}";
